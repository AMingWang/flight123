{
  "name": "flight-proxy-app",
  "version": "1.0.0",
  "description": "CORS proxy for OpenSky Network.",
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "engines": {
    "node": "18.x"
  },
  "scripts": { 
    "start": "node api/proxy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
